.build/
.swiftpm/
# SwiftPM's local editable dependency link; never include it in a release.
/Packages/spice-swift
DerivedData/
*.xcodeproj/xcuserdata/
*.app
# Build output / release artifacts (the .app, packaged .zip, checksum).
/build/
.DS_Store
.claude/
# Never commit a connection file — it carries a live ticket + CA.
*.vv
